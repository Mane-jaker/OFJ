# Skill Registry — OFJ

Generated: 2026-05-26
Project: OFJ
Persistence: engram

## User-Level Skills

| Skill | Trigger | Path | Compact Rules |
|-------|---------|------|---------------|
| branch-pr | creating, opening, or preparing PRs for review | `~/.config/opencode/skills/branch-pr/SKILL.md` | • Every PR MUST link an approved issue (status:approved label)<br>• Every PR MUST have exactly one type:* label<br>• Automated checks must pass before merge<br>• Branch names: `^(feat\|fix\|chore\|docs\|style\|refactor\|perf\|test\|build\|ci\|revert)\/[a-z0-9._-]+$`<br>• PR body must include: linked issue, PR type, summary, changes table, test plan, contributor checklist<br>• Conventional commit format; no `Co-Authored-By` trailers |
| chained-pr | PRs over 400 lines, stacked PRs, review slices | `~/.config/opencode/skills/chained-pr/SKILL.md` | • Split PRs over 400 changed lines unless maintainer accepts size:exception<br>• Each PR reviewable in ≤60 minutes<br>• One deliverable work unit per PR; keep tests/docs with unit<br>• State start, end, prior deps, follow-up, out-of-scope in every chained PR<br>• Every child PR must include a dependency diagram marking current PR with 📍 |
| cognitive-doc-design | writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs | `~/.config/opencode/skills/cognitive-doc-design/SKILL.md` | • Front-load key information: what, why, how, when<br>• One idea per paragraph; use hierarchies (H2/H3)<br>• Prefer tables and lists over prose for reference data<br>• Use examples over abstraction<br>• Keep code blocks runnable and tested |
| comment-writer | PR feedback, issue replies, reviews, Slack messages, or GitHub comments | `~/.config/opencode/skills/comment-writer/SKILL.md` | • Warm and direct tone — neither cold nor overly effusive<br>• Frame feedback around the code, not the person<br>• Explain WHY something is a problem before suggesting HOW to fix<br>• Use bullet lists for multiple points; ask at most one question per comment<br>• Default to short answer; expand only when asked |
| go-testing | Go tests, go test coverage, Bubbletea teatest, golden files | `~/.config/opencode/skills/go-testing/SKILL.md` | • Prefer table-driven tests with `t.Run(tt.name, ...)`<br>• Test behavior and state transitions, not implementation<br>• Use `t.TempDir()` for filesystem tests<br>• Integration tests skippable with `testing.Short()`<br>• Bubbletea: test `Model.Update()` directly; use teatest only for interactive flows |
| issue-creation | creating GitHub issues, bug reports, or feature requests | `~/.config/opencode/skills/issue-creation/SKILL.md` | • Blank issues disabled — MUST use template<br>• Every issue gets `status:needs-review` on creation<br>• Maintainer must add `status:approved` before any PR<br>• Questions go to Discussions, not issues<br>• Fill ALL required fields; check pre-flight checkboxes |
| judgment-day | judgment day, dual/adversarial review, juzgar | `~/.config/opencode/skills/judgment-day/SKILL.md` | • Resolve project skills before launching judges<br>• Launch two blind judges in parallel with identical target and criteria<br>• Never review the code yourself<br>• Classify warnings as real only if normal intended use can trigger them<br>• Ask before fixing Round 1 confirmed issues |
| skill-creator | new skills, agent instructions, documenting AI usage patterns | `~/.config/opencode/skills/skill-creator/SKILL.md` | • Required structure: frontmatter, Activation Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References<br>• Description: quoted, one line, trigger-first, ≤250 chars<br>• Target 180-450 body tokens; move examples into references/<br>• Hard rules must be observable; Decision Gates cover real forks<br>• Quality gates: output contract states exactly what to return, references resolve locally |
| work-unit-commits | implementation, commit splitting, chained PRs, keeping tests/docs with code | `~/.config/opencode/skills/work-unit-commits/SKILL.md` | • Each commit is a reviewable unit: one logical change + tests + docs<br>• Keep PRs under 400 changed lines<br>• Commit message explains WHAT and WHY, not HOW<br>• Tests and docs ship in the SAME commit as the code they verify<br>• Split across chained PRs when SDD budget says High |
| find-skills | user asks "how do I do X", "find a skill for X", or expresses interest in extending capabilities | `~/.claude/skills/find-skills/SKILL.md` | • Match user request to installed or available skills<br>• Search skill ecosystem for matching capabilities<br>• Guide installation of matched skills<br>• Recommend skill when request matches non-trivial workflow<br>• Do NOT load this skill for trivial requests |
| insforge | writing app code with InsForge/@insforge/sdk: CRUD, auth, storage, functions, AI, realtime, emails, Stripe | `~/.agents/skills/insforge/SKILL.md` | • Use `@insforge/sdk` for client-side operations<br>• Set env vars: `NEXT_PUBLIC_INSFORGE_URL` + `NEXT_PUBLIC_INSFORGE_ANON_KEY`<br>• Anon key for SDK clients (including SSR); API key is admin-level, server-only<br>• For infrastructure/SQL/migrations, use insforge-cli skill<br>• Modules: Database (CRUD+RPC), Auth (login/OAuth), Storage (upload/download), Functions (invoke), AI (OpenRouter), Realtime (subscribe/publish), Email, Payments (Stripe) |
| insforge-cli | managing InsForge infrastructure: SQL, migrations, RLS, functions, storage, deployments, secrets, Stripe, cron, logs | `~/.agents/skills/insforge-cli/SKILL.md` | • NEVER install globally — always use `npx @insforge/cli`<br>• Auth: `login`, `whoami`, `logout`; use `--email` for non-interactive login<br>• Project: `create`, `link`, `current`, `list`, `metadata`<br>• DB: `db` subcommands for SQL/migrations; `config apply` for `insforge.toml`<br>• Exit codes: 0=ok, 1=error, 2=not authd, 3=no project, 4=not found, 5=permission<br>• Use `--json` for structured output AND to skip prompts; combine with `-y` for destructive ops |
| insforge-debug | errors, bugs, performance issues, unexpected behavior in InsForge projects | `~/.agents/skills/insforge-debug/SKILL.md` | • Always use `npx @insforge/cli` — never global install<br>• Fastest path: `npx @insforge/cli diagnose --ai "<issue>"`<br>• Triage: match symptom to scenario (SDK error, HTTP status, function timeout, slow query, auth failure, realtime, performance, deploy failure)<br>• Drill into logs: `npx @insforge/cli diagnose logs`<br>• Verify AI diagnoses against real logs before committing to fix |
| insforge-integrations | wiring external auth providers (Clerk, Auth0, WorkOS, Kinde, Stytch, Better Auth) or OKX x402 payment facilitator | `~/.agents/skills/insforge-integrations/SKILL.md` | • Auth providers: JWT-based RLS via `insforge.config.auth`<br>• Set provider env vars, configure webhook endpoints in provider dashboard<br>• Payment (OKX x402): HTTP pay-per-use billing, not Stripe<br>• Each provider has its own dedicated guide under the skill directory<br>• Test auth flow end-to-end after wiring |

## Project-Level Skills

None installed yet.

## Project Convention Files

None found (project is brand new).
